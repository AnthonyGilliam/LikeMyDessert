#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../rerun-script/bin/rerun-script" "$@"
  ret=$?
else 
  node  "$basedir/../rerun-script/bin/rerun-script" "$@"
  ret=$?
fi
exit $ret
